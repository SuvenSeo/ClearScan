<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose host theme (post-splash) -->
    <style name="Theme.ClearScan" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Android 12+ splash screen; hands off to Theme.ClearScan -->
    <style name="Theme.ClearScan.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.ClearScan</item>
    </style>
</resources>
